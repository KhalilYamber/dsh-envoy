# 协作协议（Hana 与 DSH）

本文件是 Hana 与 DSH 协作的固定规则。DSH 每次收到任务后，先读本文件再开工。

## 双方角色

- **Hana（丽娜）**：主人的通用型 agent，负责总览任务全局、判断任务归属、把 coding 类工作派给 DSH 执行并验收成果。
- **DSH**：主人的 coding 执行 agent，负责按任务书实现、自检、汇报。
- 双方协作，服务同一位主人。

## 工作标签规则

- 每次「工作」有唯一标签，格式【MMdd-NN】（如【0814-03】），同一天递增，跨天从 01 重新开始
- 派单消息开头必带标签，如 `【0814-03】请完成...`
- 同一工作内派生的所有子会话（自检等）必须复用父标签：调用 dsh-api.ps1 时用 `-Tag` 参数传入
- 计数器文件：`D:\DeepSeek-Harness\协助Hana\dsh-bridge\.work-counter.json`

## 任务书约定

- 每个工作的任务书写在 `D:\DeepSeek-Harness\协助Hana\dsh-bridge\task-specs\` 下
- 文件名：task-spec-<标签>.md，如 task-spec-0814-04.md
- 派单消息会指明任务书路径，DSH 必须先完整阅读任务书再动手

## 对齐流程

- **开工前**：DSH 读任务书后，先用 3~5 行复述任务要点（目标、关键约束、交付物、疑点），再动手。Hana 发现偏差会发消息纠正；未收到纠正则默认理解一致。
- **汇报时**：按下方「最终回复格式」汇报，并附「验收检查点」（Hana 应重点实测的内容）。
- **验收后**：Hana 把验收结论和返工点写回，DSH 据此判断是否对齐，无需口头复述确认。

## 自检要求

- 交付前必须实测运行自己的产物，验证规格的每个要点
- 自检产生的子会话用 -Tag 继承当前工作标签
- 自检失败最多重试 3 次，仍失败则如实汇报失败原因，不得伪装成功

## 最终回复格式

DSH 完成工作后，最终回复必须包含：
1. 修改点/交付物清单
2. 自检结果（每项通过/失败）
3. 计数器最终状态（若涉及标签）
4. 验收检查点（Hana 验收时应重点验证的 2~4 个点）

## 目录结构

```
D:\DeepSeek-Harness\协助Hana\dsh-bridge\
├── dsh-api.ps1          派单桥接脚本
├── .work-counter.json   标签计数器
├── COLLAB.md            本协议
├── task-specs\          任务书归档
└── tmp\                 临时文件（请求体、测试日志）
```

## 行为底线

- 不伪造自检结果
- 不删除或覆盖本协议、计数器、任务书归档
- 遇到规格歧义时，按「最小惊讶」原则选择，并在回复中说明你的理解
