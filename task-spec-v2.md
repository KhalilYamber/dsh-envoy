# 任务书 v2：为 dsh-api.ps1 增加工作标签功能

## 背景

Hana 与你的协作会产生多个会话。为了让主人在界面里一眼分辨「哪些会话属于同一次工作」，引入工作标签机制：每次工作分配唯一标签，同工作的所有会话共享，不同工作标签不同。

标签格式：【MMDD-序号】，如【0814-01】。同一天序号递增，跨天序号从 01 重新开始。

## 现状

已存在脚本：D:\DeepSeek-Harness\协助Hana\dsh-bridge\dsh-api.ps1（你上一版写的）。
计数器文件：D:\DeepSeek-Harness\协助Hana\dsh-bridge\.work-counter.json，内容 {"date":"0814","seq":1}。
表示当前已用到 0814-01，下一次自动生成应为 0814-02。

## 要新增的功能

1. **自动标签生成**：
   - run 和 prompt 子命令在无 -Tag 参数时，读取 .work-counter.json
   - 若 date 字段不等于今天（MMdd 格式），seq 重置为 1 并更新 date
   - 否则 seq + 1
   - 生成标签【MMdd-NN】，写回计数器文件
   - 消息发送前自动在文本最前面加标签前缀，例如消息变成：`【0814-02】原始消息`

2. **-Tag 参数（标签继承）**：
   - run 和 prompt 支持可选参数 -Tag "0814-02"
   - 有 -Tag 时：跳过计数器逻辑，直接使用该标签加前缀
   - 用途：同一工作内派生的子会话复用父标签

3. **子命令签名变化**：
   - `run [-Tag <标签>] <任务...>`
   - `prompt [-Tag <标签>] <sessionId> <消息...>`
   - new 和 history 不变

4. **日志**：run 在 stderr 打印使用的标签，如 [run] 标签: 0814-02

## 自检要求

1. 先跑一次带 -Tag 的 prompt 或 run：`powershell -NoProfile -File <脚本> run -Tag "0814-02" "请只回复两个字：通过"`，确认消息前缀带【0814-02】且计数器文件未被改动
2. 再跑一次不带 -Tag 的 run，确认自动生成新标签（应为 0814-03）且计数器 seq 变为 3
3. 检查计数器文件内容正确

注意：自检第 1 步你收到的任务消息开头就带当前工作标签【0814-02】，这就是你自检时应复用的标签值。

完成后在最终回复里写明：修改点清单、自检结果、计数器最终状态。
