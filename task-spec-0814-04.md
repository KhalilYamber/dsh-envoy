# 任务书 0814-04：dsh-api.ps1 健壮性加固

## 背景

先读 `D:\DeepSeek-Harness\协助Hana\dsh-bridge\COLLAB.md` 协作协议（本工作即按其规则运行）。

dsh-api.ps1 已具备：new / prompt / history / run 四个子命令、工作标签自动生成（-Tag 继承、计数器）。本次加固三处健壮性短板，防止后续协作踩坑。

## 加固项

### 1. 服务健康检查

现象：DSH 服务未运行（被闲置看护杀掉）时，curl 报一堆底层错误，Hana 无法快速判断问题。

要求：
- run / prompt / new 在执行任何请求前，先探测 http://127.0.0.1:3080 是否可连
- 不可连时，输出一行清晰中文错误并退出码 3，例如：
  `[错误] DSH 服务未运行（http://127.0.0.1:3080 不可达）。请先双击桌面 DeepSeek Harness 快捷方式启动服务，再重试。`
- 探测方式自选（curl -o NUL 或 Test-NetConnection），超时不超过 3 秒

### 2. 计数器自愈

现象：.work-counter.json 被误删或内容损坏时，Get-NextWorkTag 报错，标签体系瘫痪。

要求：
- 读取计数器时，若文件不存在、JSON 解析失败、或缺少 date/seq 字段，自动重建：
  `{"date":"<今天MMdd>","seq":1}`，并在 stderr 提示 `[提示] 计数器缺失或损坏，已重建`
- 重建后本次生成的标签应为【今天-02】（即先重建再正常递增，与正常路径行为一致）

### 3. 并发取号互斥锁

现象：两个派单同时运行时，可能读到同一 seq，生成重复标签。

要求：
- 取号+写回计数器的过程用命名互斥锁保护（System.Threading.Mutex，名称如 'Local\DSH-Bridge-Counter'）
- 锁内完成：读计数器 → 计算新值 → 写回 → 返回标签
- 等待锁超时 10 秒，超时报错退出码 4

### 4. 状态输出优化（顺带）

现象：run 的中间状态输出到 stderr，Hana 用 PowerShell 管道接收时显示为红色 NativeCommandError 噪音。

要求：
- run 的中间状态（[run] 标签、创建会话、轮询等）改用 Write-Host 输出（不进管道、不触发错误流）
- 最终回复文本仍用 Write-Output（进标准输出，供管道接收）
- prompt / new / history 的正常结果保持 Write-Output

## 自检要求

1. 健康检查：停掉一个不存在的服务场景难以模拟，改为验证「服务在跑时不误报、逻辑分支存在」。可用临时把 BaseUrl 改成错误端口自测，再改回（自测后必须恢复正确值）
2. 计数器自愈：把 .work-counter.json 改名备份后跑一次 run，确认重建提示出现、标签生成正常；然后删除重建的文件、把备份恢复原样
3. 互斥锁：代码审查级别自检（起两个并发进程模拟有风险，可只验证锁逻辑存在且语法正确）
4. 输出优化：跑 run，确认中间状态不再以错误流噪音形式出现

自检子会话用 -Tag 0814-04（见 COLLAB.md）。

最终回复按 COLLAB.md 格式：修改点清单、自检结果、计数器最终状态。
