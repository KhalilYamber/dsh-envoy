# 任务书 0814-05：修复 run 子命令的完成判定语义

## 背景

先读 `D:\DeepSeek-Harness\协助Hana\dsh-bridge\COLLAB.md` 协作协议（本次起对齐流程生效，开工前先复述要点）。

## 缺陷描述

当前 run 子命令的轮询逻辑是「出现第一条新的 assistant/message 就返回」。但你在收到任务后通常会先回一句（如「任务书已阅，先考察现有脚本」），这句只是开场白，任务还没做完。run 把它当最终结果返回，导致 Hana 拿到中间消息。

## 修复要求

1. **完成判定改为回合结束**：轮询 history 时，判定「任务完成」的条件改为：出现 seq 大于派单时最大 seq 的 **turn/end 事件**
2. **最终回复提取**：找到该 turn/end 之前、派单之后，最后一个 content 中含 type=text 且文本非空的 assistant/message，输出其文本
3. **中间消息不再触发返回**：开场白、中间汇报等一律无视，继续轮询
4. **超时**：默认超时从 600 秒放宽到 1800 秒（30 分钟），因为 coding 任务可能多轮执行较久。超时行为保持：stderr 提示已超时，退出码 2
5. **轮询日志**：每次轮询在 Write-Host 输出当前状态，如 `[run] 第 3 次查询：任务仍在进行（已等待 18 秒，最新事件 seq 1234）`

## 自检要求

1. 构造一个「多回合」任务验证新逻辑：派一个会先回一句、再继续干活的任务。可用这个方法：run 派单「请先用一句话说你开始工作了，然后调用工具列出当前目录内容，最后回复：任务完成」。确认 run 不会在开场白返回，而是等到 turn/end 后返回最终回复
2. 验证超时参数可生效（可通过代码审查确认，不必真等 30 分钟）
3. 验证开场白场景：run 派单「请只回复一句：已收到」，确认仍在 turn/end 后正常返回
4. 自检子会话用 -Tag 0814-05

最终回复按协议格式：修改点清单、自检结果、计数器最终状态、验收检查点。
