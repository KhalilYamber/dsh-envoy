# 任务书 0814-06：新增 status 子命令（阶段二核心）

## 背景

先读 `D:\DeepSeek-Harness\协助Hana\dsh-bridge\COLLAB.md` 协作协议。

阶段二目标：让 Hana 随时能查看你的工作进度、感知主人的插话。实现方式选定为「按需轮询」，即给 dsh-api.ps1 增加 status 子命令。

## 执行流程（重要）

1. 读完协议和任务书后，**先输出 3~5 行要点复述**（目标、关键约束、疑点），然后**停下，等待 Hana 的下一条消息**
2. 收到 Hana 确认或纠正后，再动手实现
3. 实现完成、自检通过后，按协议格式做最终汇报

## 功能规格：status <sessionId>

输入：会话 id。

输出以下摘要（全部 UTF-8，中文）：

```
会话: <sessionId>
状态: 进行中 / 已完成 / 空会话
事件: 共 N 条，最新事件 <type>，时间 <HH:mm:ss>
最近助手: <最后一条含非空 text 的 assistant/message 文本，截断 120 字符>
最近用户: <最后一条含非空 text 的 user/message 文本，截断 120 字符，若没有则不显示此行>
```

判定规则：
- 事件数为 0 或仅有初始事件 → 空会话
- turn/start 计数 > turn/end 计数 → 进行中
- 否则 → 已完成

## 技术要求

- 复用现有函数：Assert-DshServiceUp、信封构造、UTF-8 输出约定
- 提取文本的字段路径注意：assistant/message 的文本在 data.message.content 下（type=text 的段）；user/message 的文本在 data.content 下。以实测为准
- 时间用本地时间（HH:mm:ss），来源是事件的 time 字段（Unix 毫秒）
- 不修改任何现有子命令行为

## 自检要求

1. 用已完成会话测：任选一个今天已结束的会话 id，确认状态=已完成、摘要正确
2. 用进行中会话测：先手动 API 派一个多回合任务（如「先回复开始工作，再列目录，最后回复完成」），在其执行期间跑 status，确认状态=进行中；回合结束后再跑，确认变已完成。此自检子会话用 -Tag 0814-06
3. 确认空会话场景：新建会话后立即 status，状态=空会话

最终回复按协议格式：修改点清单、自检结果、计数器状态、验收检查点。
